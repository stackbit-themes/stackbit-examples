The source files for unpic-imc (core & react packages) are temporarily included here. 
The code includes the fixes in https://github.com/ascorbic/unpic-img/pull/13.

Once the PR is merged, this directory can be deleted along with the "paths" attribute in ../tsconfig.json.
